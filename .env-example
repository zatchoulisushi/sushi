# Supabase Configuration (SERVER-SIDE ONLY)
# These are only used at build time for Content Layer data loading
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# App Configuration  
VITE_APP_URL=http://localhost:4321
VITE_RESTAURANT_NAME=Osushi
VITE_RESTAURANT_PHONE=01 23 45 67 89
VITE_RESTAURANT_EMAIL=contact@osushi.fr

# Note: VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY are no longer needed
# All database access is now done at build-time through Content Layer
# For authentication and dynamic operations, implement API endpoints