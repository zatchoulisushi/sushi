---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout>
  <Header />
  <main class="pt-16 min-h-screen">
    <section class="py-20 px-4 sm:px-6 lg:px-8">
      <div class="max-w-4xl mx-auto text-center">
        <h1 class="text-4xl sm:text-5xl font-bold mb-6 bg-gradient-to-r from-white to-primary-500 bg-clip-text text-transparent">
          Profil Utilisateur
        </h1>
        <div class="bg-white/5 backdrop-blur-sm rounded-2xl p-8 mb-8">
          <div class="text-6xl mb-4">👤</div>
          <h2 class="text-2xl font-bold text-white mb-4">Fonctionnalité non disponible</h2>
          <p class="text-white/70 mb-6">
            La gestion des profils utilisateurs nécessite une authentification backend qui n'est pas disponible 
            dans cette version statique utilisant Astro Content Layer.
          </p>
          <p class="text-white/60 text-sm mb-6">
            Pour activer cette fonctionnalité, vous devriez implémenter un système d'authentification côté serveur 
            ou utiliser Astro en mode SSR (Server-Side Rendering).
          </p>
          <a href="/menu" class="inline-flex items-center px-6 py-3 bg-primary-600 hover:bg-primary-700 text-white rounded-xl transition-all duration-300 hover:scale-105">
            Retour au menu
          </a>
        </div>
      </div>
    </section>
  </main>
  <Footer />
</Layout>